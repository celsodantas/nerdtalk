<%= form_for(@proposal) do |f| %>
  <% if @proposal.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@proposal.errors.count, "error") %> prohibited this proposal from being saved:</h2>

      <ul>
      <% @proposal.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

	<fieldset id="" class="">
	  <div class="field">
	    <%= f.label :title, "Título" %><br />
	    <%= f.text_field :title, :style => "width: 100%" %>
	  </div>
	  <div class="field">
	    <%= f.label :description, "Descrição" %>
		<small class="pull-right">Formatação de texto: <a href="http://en.wikipedia.org/wiki/Textile_(markup_language)" target="_blank">Textile (Wikipedia)</small>
	    <%= f.text_area :description, :style => "width: 100%" %>
	  </div>
	  <div class="actions">
	    <%= f.submit "Adicionar ideia", :class => "btn btn-primary" %>
	  </div>
	</fieldset>
<% end %>
