# SQLite version 3.x
#   gem 'activerecord-jdbcsqlite3-adapter'
#
# Configure Using Gemfile
# gem 'activerecord-jdbcsqlite3-adapter'
#
development:
    encoding: utf8
    database: nerd_talk_dev
    adapter: mysql
    host: localhost
    username: root
    password: root

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
    encoding: utf8
    database: nerd_talk_test
    adapter: mysql
    host: localhost
    username: root
    password: root

production:
    encoding: utf8
    database: nerd_talk_production
    adapter: mysql
    host: localhost
    username: root
    password: root
